var React     = require('react');
var GroupList = require('../components/GroupList.react.js');

var mountNode = document.getElementById('app');
React.render(React.createElement(GroupList), mountNode);
